# Production API URL — set this in Vercel Dashboard (Project Settings → Environment Variables).
# When the backend is on a different domain, point to it directly.
# When empty/unset, the frontend uses same-origin relative paths (/chat, /products).
VITE_API_URL=https://your-backend-url.example.com
